import { View } from 'react-native';
import React from 'react';

const Separator = () => (
    //Component renders sepeartor between list items on the screen
    <View style={{ width: '100%', height: 20}}/>
);

export default Separator;